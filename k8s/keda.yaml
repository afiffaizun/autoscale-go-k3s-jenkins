apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: go-app-scaledobject
  namespace: go-cicd
spec:
  scaleTargetRef:
    name: go-app

  minReplicaCount: 1
  maxReplicaCount: 5

  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "50"
